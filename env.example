# OxideBot 环境变量配置
# 格式为 KEY=value，一行一个。这里不是 shell 脚本，$()、反引号等不会被执行。

# 必填：从 BotFather 获取的 Telegram Bot Token
TELEGRAM_BOT_TOKEN=

# 日志级别：error / warn / info / debug / trace
RUST_LOG=info

# 卸载模块时默认保留配置和数据库；改为 1 才会同时清除 /data/adb/<moduleId>
REMOVE_DATA_ON_UNINSTALL=0
